import { notFound } from "next/navigation";
import { ActionLink, Badge, Card, PageHeader, Shell, TableShell } from "@/components/ui";
import { requirePermission, tenantFilter } from "@/lib/authz";
import { tenantTransaction } from "@/lib/tenant-db";

export const dynamic="force-dynamic";
export default async function AnalysisPage({params}:{params:Promise<{analysisId:string}>}){const user=await requirePermission("security.analyses.read");const tenantId=tenantFilter(user);if(!tenantId)notFound();const {analysisId}=await params;const analysis=await tenantTransaction(tenantId,(tx)=>tx.securityAnalysis.findFirst({where:{id:analysisId,tenantId},include:{findings:true,report:true,fortigate:{include:{customer:true}},sourceBackup:true}}));if(!analysis)notFound();return <Shell><PageHeader title={`Analyse ${analysis.configSha256.slice(0,12)}`} description={`${analysis.fortigate.customer.name} - ${analysis.fortigate.hostname??"FortiGate"}`} actions={analysis.report?<ActionLink href={`/api/security/reports/${analysis.report.id}`} variant="primary">PDF downloaden</ActionLink>:undefined}/><div className="grid gap-4 sm:grid-cols-4"><Card title="Status" value={analysis.status}/><Card title="Score" value={analysis.score??"-"}/><Card title="Critical" value={analysis.criticalCount}/><Card title="High" value={analysis.highCount}/></div>{analysis.safeSummary?<section className="mt-6 rounded-xl border border-border bg-surface p-5"><h2 className="font-display text-xl font-semibold">Managementsamenvatting</h2><p className="mt-3 max-w-4xl whitespace-pre-wrap leading-7">{analysis.safeSummary}</p></section>:null}<TableShell className="mt-6"><table className="table-pro w-full min-w-[900px] text-left text-sm"><thead><tr><th>Severity</th><th>Regel</th><th>Uitleg</th><th>Veilig bewijs</th><th>Hersteladvies</th></tr></thead><tbody>{analysis.findings.map((f)=><tr key={f.id} className="border-t border-border align-top"><td><Badge tone={f.severity==="CRITICAL"||f.severity==="HIGH"?"danger":f.severity==="MEDIUM"?"warning":"neutral"}>{f.severity}</Badge></td><td>{f.ruleId}<br/>{f.title}</td><td>{f.explanation}</td><td className="font-mono text-xs">{f.evidence}</td><td>{f.remediation}</td></tr>)}</tbody></table></TableShell></Shell>}
