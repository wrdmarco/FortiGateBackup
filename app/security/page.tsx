import { ActionLink, Badge, Card, PageHeader, Shell, TableShell } from "@/components/ui";
import { requirePermission, tenantFilter } from "@/lib/authz";
import { tenantSecurityOverview } from "@/lib/security/queries";

export const dynamic="force-dynamic";
export default async function SecurityPage(){const user=await requirePermission("security.analyses.read");const tenantId=tenantFilter(user);if(!tenantId)throw new Error("Selecteer een klanttenant.");const overview=await tenantSecurityOverview(tenantId);return <Shell><PageHeader title="FortiGate beveiliging" description="Actuele, tenantgebonden analyse van de nieuwste gewijzigde configuratie per FortiGate." actions={<ActionLink href="/settings?tab=foundry">Azure Foundry instellen</ActionLink>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card title="Gemiddelde score" value={overview.average??"Niet beschikbaar"}/><Card title="Scoretrend" value={overview.trend===null?"Niet beschikbaar":`${overview.trend>=0?"+":""}${overview.trend}`}/><Card title="Analysedekking" value={`${overview.coverage.analysed} van ${overview.coverage.total}`}/><Card title="Critical / high" value={`${overview.critical} / ${overview.high}`}/><Card title="Onder drempel" value={overview.belowThreshold} detail="Score lager dan 70"/><Card title="In behandeling" value={overview.pending}/><Card title="Mislukt / geblokkeerd" value={overview.failed}/></div><TableShell className="mt-6"><table className="table-pro w-full min-w-[760px] text-left text-sm"><thead><tr><th>FortiGate</th><th>Nieuwste wijziging</th><th>Status</th><th>Score</th><th>Actie</th></tr></thead><tbody>{overview.devices.map(({device,backup,analysis})=><tr key={device.id} className="border-t border-border"><td>{device.hostname??device.id}</td><td>{backup?backup.createdAt.toLocaleString("nl-NL",{timeZone:"UTC"}):"Geen gewijzigde backup"}</td><td><Badge tone={analysis?.status==="COMPLETED"?"success":analysis?.status==="FAILED"||analysis?.status==="BLOCKED"?"danger":"warning"}>{!analysis?"Niet geanalyseerd":analysis.status}</Badge></td><td>{analysis?.status==="COMPLETED"?analysis.score:"-"}</td><td><ActionLink href={`/customers/${device.customerId}/fortigates/${device.id}/security`}>Historie</ActionLink></td></tr>)}</tbody></table></TableShell></Shell>}
